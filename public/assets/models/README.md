# 3D Models

Place the current character model here as:

`AxStarr.glb`

Production path used by the site:

`public/assets/models/AxStarr.glb`

When the rigged replacement is ready, keep the same filename or update the model URL in `src/components/StarrScene.jsx`.
